This file is automatically generated and should not be edited manually.

It records the installed CLI version and the authoritative inventory of files and directories bootstrapped by the CLI for this platform.

- Version: {{package.version}}
- Repository: {{package.repository.url}}
- Maintainer: {{package.author.name}} ({{package.author.email}})

`managed_files` and `managed_directories` in the YAML frontmatter are the source of truth used by `update` and `debloat`.