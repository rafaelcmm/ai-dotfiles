# /static

This folder contains all static files for ai tools configuration

## .copilot

Contains the `copilot` tool configuration to be used in a user's home directory.

## .cursor

Contains the `cursor` tool configuration to be used in a user's home directory.

## .claude

Contains the `claude` tool configuration to be used in a user's home directory.